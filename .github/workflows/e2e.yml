# E2E does NOT currently gate Pages deploy (see deploy-pages.yml V10 note).
# Treat failures as release blockers on the same SHA anyway — do not ship red E2E.
# Prefer failing fast with evidence (traces under docs/traces/) for hidden-input timeouts.
name: E2E

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: e2e-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  playwright:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium
      - name: Build
        run: npm run build
      - name: E2E
        run: npm run test:e2e
        env:
          CI: '1'
      - name: Upload traces & screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v4-e2e-artifacts
          path: |
            docs/screenshots/v4/
            docs/traces/v4/
          if-no-files-found: ignore
