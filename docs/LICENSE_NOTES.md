# License notes

仓库当前 **没有** 根目录 `LICENSE` 文件；`package.json` 亦未声明 `license` 字段。README 仅写「仅供学习交流使用」，**不能**据此推断为 MIT 或其它 OSI 许可，也 **不应** 冒用他人/组织版权声明。

## 建议（需版权人确认后执行）

1. 明确著作权归属（个人 / 课程组 / 组织）。  
2. 若希望开放复用，由权利人选择并添加标准 SPDX 文本，例如 MIT 或 Apache-2.0，写入根目录 `LICENSE`，并在 `package.json` 填写 `"license"`。  
3. 若仅课程内部使用，可添加明确的「All rights reserved / 仅供课程学习」声明，避免贡献者误以为可任意再许可。  
4. 第三方依赖遵循各自 `node_modules` 内许可证；构建产物再分发时需一并遵守。

**本文件不是授权许可，仅为状态说明与行动建议。**
